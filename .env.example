# TrustTunnel Configuration

# The hostname for the VPN endpoint (e.g., vpn.yourdomain.com)
TT_HOSTNAME=vpn.example.com

# Credentials for the admin user (format: username:password)
TT_CREDENTIALS=admin:securepassword123

# Certificate Type: 'self-signed' or 'letsencrypt'
TT_CERT_TYPE=self-signed

# Email for Let's Encrypt (Required if TT_CERT_TYPE is 'letsencrypt')
TT_ACME_EMAIL=admin@example.com

# Listen Address (Default: 0.0.0.0:443)
TT_LISTEN_ADDRESS=0.0.0.0:443

# Use Let's Encrypt Staging environment (true/false) - for testing
TT_ACME_STAGING=false

# Host Ports (Change these if you have conflicts, e.g., with an existing web server)
TT_PORT_TCP=443
TT_PORT_UDP=443
